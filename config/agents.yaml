# /config/agents.yaml

default: "default-agent"

agents:
  default-agent:
    description: "A minimal agent with no special instructions."
    llm: "gpt-4o-mini"
    allowed_tools:
      - fetch_url
      - http_get
      - math
      - text
    max_steps: 8