def echo(text: str) -> str:
    """
    Simple echo skill.
    Pure, deterministic, no side effects.
    """
    return text


def add(a: float, b: float) -> float:
    """
    Simple add skill.
    """
    return a + b
