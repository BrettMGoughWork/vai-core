from tools.code_analysers.shared.rule_base import Rule

class TypeInvariantsRule(Rule):
    id = "S1-TYPE-INVARIANTS"
    description = "Ensure type invariants for core dataclasses."

    def run(self, ctx):
        # Placeholder — fill in once invariants are fully defined
        return []